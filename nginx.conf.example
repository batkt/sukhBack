# Add this inside your server { } block to fix 413 Request Entity Too Large
# when uploading images (gomdol/sanal) from the app:
#
#   client_max_body_size 20M;
#
# Example full server block:
#
# server {
#     listen 80;
#     server_name amarhome.mn;
#     client_max_body_size 20M;   # <-- Add this line
#
#     location /api {
#         proxy_pass http://127.0.0.1:8084;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
#
# After editing: sudo nginx -t && sudo nginx -s reload
